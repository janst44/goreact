Full stack react + go for todo items. With multi-user auth.

to run server: air
to run client: npm run dev

swag init --parseInternal --dir cmd/api/ --parseDependency --dir cmd/api,internal/database --output docs